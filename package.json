{
  "name": "bun-webview",
  "module": "src/index.ts",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "build": "bun scripts/build.ts",
    "clean": "bun exec \"rm -rf webview/build\"",
    "hidecmd":"./scripts/hidecmd.bat",
    "postinstall": "bun run build"
  },
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
